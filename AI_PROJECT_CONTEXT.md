# AI PROJECT CONTEXT: FlatPatternStretcher

**Этот файл содержит полный контекст проекта для AI-ассистентов.**  
Используйте его при работе над новыми задачами, чтобы сохранить архитектуру и стиль проекта.

---

## 📋 ОПИСАНИЕ ПРОЕКТА

**FlatPatternStretcher** - автоматизированная система для обработки разверток листового металла из КОМПАС-3D.

### Основные функции:
1. **Растяжение разверток** по длине с сохранением дуг (arc1, arc2)
2. **Создание внешнего радиуса из внутреннего** (автоматический расчёт с учётом толщины и радиуса гибки)
3. **Пакетная обработка** с автоматическим сопоставлением разверток и оснований
4. **Интеграция с КОМПАС-3D** через COM API

### Целевая аудитория:
Инженеры-конструкторы, работающие с листовым металлом в КОМПАС-3D.

---

## 🏗️ АРХИТЕКТУРА

### Структура проекта:
```
FlatPatternStretcher/
├── app.py                      # GUI приложение (CustomTkinter)
├── core/                       # Основные модули
│   ├── dxf_processor.py       # Обработка DXF (растяжение, измерение)
│   ├── base_analyzer.py       # Анализ оснований (arc1, arc2)
│   ├── flat_pattern_service.py # Сервис для работы с развертками
│   └── kompas_connector.py    # Интеграция с КОМПАС-3D
├── create_outer_from_inner.py # Скрипт создания внешнего из внутреннего
├── requirements.txt           # Зависимости Python
└── test/                      # Тестовые DXF файлы
```

### Принципы архитектуры:
1. **Разделение ответственности**: каждый модуль отвечает за свою область
2. **Слоистость**: GUI → Service → Processor → ezdxf
3. **Независимость**: модули `core/` могут работать без GUI
4. **Расширяемость**: легко добавить новые форматы или операции

---

## 🔧 КЛЮЧЕВЫЕ ТЕХНИЧЕСКИЕ РЕШЕНИЯ

### 1. Работа с DXF (dxf_processor.py)

**Библиотека**: `ezdxf` (версия >=1.0.0)

**Основные операции:**
- `load()` - загрузка DXF и определение габаритов
- `stretch()` - растяжение по указанной оси
- `_get_zones()` - определение зон растяжения (stretch/fixed)
- `_apply_mapping()` - применение трансформации к геометрии

**Алгоритм растяжения:**
```python
1. Определить границы (extents) чертежа
2. Найти дуги (ARC) - они определяют фиксированные зоны
3. Создать зоны:
   - fixed_zones: области с дугами (не растягиваются)
   - stretch_zones: пространство между фиксированными зонами
4. Построить mapping (old_coord → new_coord)
5. Применить трансформацию ко всем элементам:
   - LINE: оба конца
   - ARC: center
   - SPLINE: control_points
   - LWPOLYLINE: точки с учётом bulge
```

**Важно**: Дуги (ARC) остаются неизменными, растягивается только пространство между ними!

### 2. Анализ оснований (base_analyzer.py)

**Назначение**: Автоматически находит на "основании" две дуги (arc1, arc2) и их параметры.

**Алгоритм:**
```python
1. Найти все ARC в файле
2. Отсортировать по радиусу (по убыванию)
3. Две самые большие дуги = arc1 (внешняя) и arc2 (внутренняя)
4. Измерить:
   - arc_length (длина дуги в мм)
   - width (Y-размер чертежа)
   - radius (радиус дуги)
```

**Использование**: Для сопоставления разверток с основаниями по длине дуг.

### 3. Интеграция с КОМПАС-3D (kompas_connector.py)

**Технология**: COM API через `win32com.client`

**Операции:**
- `open_document()` - открыть файл в КОМПАС
- `export_to_dxf()` - экспортировать активный документ в DXF
- `close_kompas()` - закрыть КОМПАС

**Обработка ошибок**: Используется defensive programming (try-except с информативными сообщениями).

### 4. GUI (app.py)

**Фреймворк**: CustomTkinter (современный Material Design для Tkinter)

**Архитектура GUI:**
- 2 вкладки: "Одна развертка" и "Пакетная обработка"
- Логирование в текстовое поле с автопрокруткой
- Цветовая схема: dark mode с акцентами

**Паттерны:**
- Все операции запускаются в основном потоке (GUI не блокируется из-за быстрой работы)
- Логирование через метод `_log()` с эмодзи для читаемости
- Validation ввода перед обработкой

---

## 📐 РАБОТА С DXF: ОСОБЕННОСТИ

### Системы координат:
- **Абсолютные координаты**: X, Y в мм (из `entity.dxf.center`, `entity.dxf.start` и т.д.)
- **Относительные координаты**: (x - min_x) / length_x ∈ [0, 1]

### Типы элементов:
```python
LINE      # Прямые линии (start, end)
ARC       # Дуги (center, radius, start_angle, end_angle)
SPLINE    # Сплайны (control_points, fit_points)
LWPOLYLINE # Легковесная полилиния (points с bulge)
CIRCLE    # Окружности (center, radius)
TEXT/MTEXT # Текст (игнорируем при анализе геометрии!)
```

### Трансформации:
```python
# Масштабирование по оси X
scale_x = target_length / current_length
transform = Matrix44.scale(scale_x, 1, 1)
entity.transform(transform)

# Смещение
transform = Matrix44.translate(dx, dy, 0)
entity.transform(transform)
```

### Габариты (extents):
```python
from ezdxf import bbox

extents = bbox.extents(modelspace)
min_x = extents.extmin.x
max_x = extents.extmax.x
length_x = max_x - min_x
```

---

## 🎨 СТИЛЬ КОДИРОВАНИЯ

### Python Style Guide:
- **PEP 8** с отступами 4 пробела
- **Type hints** для публичных методов
- **Docstrings** для классов и публичных методов
- **Русские комментарии** (проект для русскоязычных пользователей)

### Naming Conventions:
```python
# Классы: PascalCase
class DxfProcessor:

# Методы: snake_case
def load_file(self, path: str) -> Dict:

# Приватные методы: _snake_case
def _apply_mapping(self, mapping: Dict):

# Константы: UPPER_SNAKE_CASE
DEFAULT_TOLERANCE = 0.001
```

### Логирование:
```python
# В GUI используем эмодзи для читаемости
print(f"✅ Файл загружен: {filename}")
print(f"⚠️  Предупреждение: {warning}")
print(f"❌ Ошибка: {error}")
print(f"📏 Длина: {length:.3f} мм")
```

### Обработка ошибок:
```python
try:
    # Опасная операция
    doc = ezdxf.readfile(path)
except Exception as e:
    # Информативное сообщение
    print(f"❌ Не удалось открыть файл {path}: {e}")
    return None
```

---

## 🧮 МАТЕМАТИКА И АЛГОРИТМЫ

### 1. Расчёт внешнего радиуса из внутреннего

**Формула:**
```python
outer_length = inner_length + 2 * π * (bend_radius + thickness / 2)
```

Где:
- `inner_length` - длина дуги внутреннего радиуса (мм)
- `bend_radius` - радиус гибки (мм)
- `thickness` - толщина листа (мм)

**Обоснование**: При гибке листа металла нейтральная линия смещается, внешний радиус = внутренний + 2 × полуокружность нейтрального слоя.

### 2. Сопоставление разверток с основаниями

**Алгоритм:**
```python
1. Для каждого основания извлечь длины arc1 и arc2
2. Для каждой развертки измерить длины дуг
3. Сопоставить по минимальному расстоянию:
   distance = |flat_arc_length - base_arc_length|
4. Выбрать пару с минимальным distance
```

**Tolerance**: 5 мм (две дуги считаются одинаковыми, если разница < 5 мм)

### 3. Растяжение с фиксированными зонами

**Проблема**: Нужно растянуть чертёж, но сохранить дуги неизменными.

**Решение**: Разделить координаты на зоны:
```
|----stretch----|-fixed-|----stretch----|-fixed-|----stretch----|
    zone 1       arc1      zone 2       arc2       zone 3
```

**Mapping**:
- В фиксированной зоне: `new_x = old_x + offset`
- В зоне растяжения: `new_x = old_x * scale_factor + offset`

---

## 🔗 ЗАВИСИМОСТИ

### Обязательные:
```
ezdxf>=1.0.0         # Работа с DXF
customtkinter>=5.0.0  # GUI
pywin32              # COM API для КОМПАС
```

### Опциональные:
```
pillow               # Для иконок в GUI (если используются)
```

### Python:
- **Минимум**: Python 3.8
- **Рекомендуется**: Python 3.10+

---

## 🧪 ТЕСТИРОВАНИЕ

### Тестовые файлы:
В папке `test/` хранятся:
- **Основания** (с дугами arc1, arc2)
- **Развертки внешнего радиуса**
- **Развертки внутреннего радиуса**

### Проверка работы:
```bash
# 1. Загрузить основание и развертку
# 2. Измерить длины
# 3. Растянуть развертку
# 4. Проверить:
#    - Дуги не изменились
#    - Итоговая длина = целевой
#    - Ширина не изменилась
```

---

## 🚀 ИНСТРУКЦИИ ДЛЯ AI

### При добавлении новых функций:

1. **Сохраняйте архитектуру**:
   - Новая логика обработки → в `core/`
   - GUI элементы → в `app.py`
   - Не смешивайте UI и бизнес-логику

2. **Следуйте стилю**:
   - Русские комментарии и docstrings
   - Type hints для публичных методов
   - Эмодзи в логах для читаемости

3. **Обрабатывайте ошибки**:
   - Все операции с файлами - в `try-except`
   - Информативные сообщения об ошибках
   - Не падайте на некорректных данных

4. **Тестируйте на реальных данных**:
   - Используйте файлы из `test/`
   - Проверяйте edge cases (пустые файлы, нет дуг и т.д.)

5. **Документируйте**:
   - Обновляйте `README.md` при добавлении функций
   - Добавляйте записи в `CHANGELOG_v3.0.txt`
   - Обновляйте инструкции для пользователей

### При рефакторинге:

1. **Не ломайте API**:
   - Публичные методы должны оставаться совместимыми
   - При изменении сигнатуры - добавляйте дефолтные значения

2. **Сохраняйте имена**:
   - `arc1`, `arc2` - устоявшиеся термины
   - `stretch`, `fixed` - понятные зоны
   - Не меняйте без веской причины

3. **Пишите миграции**:
   - Если меняется формат файлов - поддерживайте старый
   - Добавляйте version checks где нужно

### При работе с DXF:

1. **Учитывайте версии DXF**:
   - `ezdxf` поддерживает R12-R2018
   - Сохраняйте в R2013 для совместимости
   - DWG 2018+ НЕ поддерживается напрямую

2. **Проверяйте entity types**:
   - Используйте `entity.dxftype()`
   - Не все элементы имеют bbox (например, TEXT)
   - Обрабатывайте исключения при работе с геометрией

3. **Координаты**:
   - Всегда проверяйте extents перед обработкой
   - Используйте относительные координаты для сопоставления
   - Абсолютные координаты - для отображения пользователю

### При работе с КОМПАС:

1. **COM API нестабилен**:
   - Всегда используйте try-except
   - Проверяйте, запущен ли КОМПАС перед операциями
   - Освобождайте ресурсы (особенно при ошибках)

2. **Пути к файлам**:
   - КОМПАС требует абсолютные пути
   - Используйте `Path.resolve()` для получения абсолютного пути
   - Проверяйте существование файлов перед открытием

---

## 📝 ЧАСТО ИСПОЛЬЗУЕМЫЕ ПАТТЕРНЫ

### 1. Загрузка и обработка DXF:
```python
from pathlib import Path
from core.dxf_processor import DxfProcessor

processor = DxfProcessor()
info = processor.load(str(file_path))

if info:
    print(f"Габариты: {info.length_x:.2f} x {info.width_y:.2f} мм")
    result = processor.stretch(target_length=1000, axis="X")
    print(f"✅ Сохранено: {result.output_file}")
```

### 2. Анализ основания:
```python
from core.base_analyzer import BaseAnalyzer

analyzer = BaseAnalyzer()
bases = analyzer.analyze_folder(folder_path)

for korpus_num, base_info in bases.items():
    print(f"{korpus_num}: arc1={base_info.arc1.length:.2f}мм")
```

### 3. Сопоставление с основанием:
```python
results = analyzer.match_and_stretch(
    folder_path=folder_path,
    anchor="center",
    axis="X"
)

for result in results:
    print(f"✅ {result.radius_type} {result.korpus_number}: {result.output_file}")
```

---

## 🎯 ТИПИЧНЫЕ ЗАДАЧИ И РЕШЕНИЯ

### Задача 1: Добавить поддержку нового формата
**Решение**: Создать новый процессор в `core/`, наследовать базовый интерфейс.

### Задача 2: Изменить алгоритм растяжения
**Решение**: Модифицировать `dxf_processor.py`, методы `_get_zones()` и `_apply_mapping()`.

### Задача 3: Добавить новую операцию
**Решение**: Добавить метод в `flat_pattern_service.py`, затем подключить в GUI.

### Задача 4: Улучшить UI
**Решение**: Модифицировать `app.py`, использовать стандартные виджеты CustomTkinter.

### Задача 5: Обработка отверстий на развертках
**Подход**:
1. Отверстия могут быть CIRCLE, ARC, LWPOLYLINE (простые) или группы LINE+SPLINE (сложные)
2. Для простых: искать по entity type
3. Для сложных: группировать близкие маленькие элементы (tolerance ~30мм)
4. Исключать TEXT/MTEXT (маркировку)
5. Отверстия должны оставаться в "фиксированных зонах" при растяжении

---

## 🔒 ОГРАНИЧЕНИЯ И ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### Ограничения:

1. **DWG формат**: `ezdxf` читает DWG только до версии 2018. Для новых версий нужен DXF.

2. **STEP/SLDPRT**: Проприетарные бинарные форматы, читаются только через API (SolidWorks API для SLDPRT).

3. **Сложная геометрия**: Если отверстия состоят из множества элементов, группировка может быть неточной.

4. **КОМПАС API**: Нестабилен, могут быть проблемы с разными версиями КОМПАС.

### Известные проблемы:

1. **Кодировка имён файлов**: Русские имена могут вызывать проблемы в git. Решение: `git config core.quotepath false`.

2. **Смещение отверстий при растяжении**: Если отверстия попадают в зону растяжения, они смещаются. Решение: определять отверстия как фиксированные зоны.

---

## 📚 ПОЛЕЗНЫЕ ССЫЛКИ

- [ezdxf Documentation](https://ezdxf.readthedocs.io/)
- [CustomTkinter Documentation](https://customtkinter.tomschimansky.com/)
- [КОМПАС-3D API](https://kompas.ru/en/support/documentation/) (требуется регистрация)
- [DXF Reference](https://www.autodesk.com/techpubs/autocad/dxf/)

---

## 🏁 ЗАКЛЮЧЕНИЕ

Этот проект демонстрирует:
- ✅ Чистую архитектуру с разделением слоёв
- ✅ Работу со сложными форматами данных (DXF)
- ✅ Интеграцию с внешними приложениями (КОМПАС через COM)
- ✅ Современный GUI на Python
- ✅ Автоматизацию рутинных инженерных задач

**При работе над новыми задачами**: читайте этот документ, следуйте установленным паттернам, сохраняйте стиль и архитектуру проекта.

---

*Последнее обновление: ноябрь 2024*

