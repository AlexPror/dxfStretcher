# FlatPatternStretcher Pro

Профессиональное приложение для автоматической коррекции длины разверток листового металла в КОМПАС-3D с поддержкой **умной пакетной обработки**.  
Использует **customtkinter** для интерфейса, **pywin32** для работы с API КОМПАС-3D и **ezdxf** для точного масштабирования плоской геометрии.

## Возможности

### 🎯 Одиночная обработка
- Загрузка исходной модели или чертежа развертки (`*.m3d`, `*.cdw`, `*.dxf`)
- Измерение текущей длины развертки по выбранной оси (X или Y)
- Выбор центра масштабирования (левый/правый край, центр; для оси Y — нижний/верхний край)
- Ввод желаемой длины и автоматический расчет коэффициента
- Поддержка удлинения и укорочения разверток

### 🤖 Автоматическая пакетная обработка
- **Автоматический анализ файлов оснований** для извлечения эталонных длин дуг
- **Интеллектуальное сопоставление** радиусов с соответствующими дугами оснований
- Обработка неограниченного количества корпусов одновременно
- Автоматическое определение типа радиуса (внешний/внутренний)
- **🆕 Проверка и выравнивание ширины разверток** (ось Y)
- **🆕 Выбор эталонной ширины** (внешний или внутренний радиус)
- Генерация детальных отчётов с полной трассируемостью

### 🔧 Технические возможности
- Автоматическое разделение DXF на «растяжимые» и «фиксированные» зоны (дуги, окружности не искажаются)
- Распределение изменения длины только по линейным участкам
- Точное сохранение формы дуг гибов
- Экспорт результата в DXF
- Валидация и подробные логи операций

### 🆕 Проверка и выравнивание ширины разверток
- **🔍 Автоматическая проверка ширины** всех разверток (допуск 0.1 мм)
- **📏 Выравнивание ширины** с выбором эталона (внешний/внутренний радиус)
- Сохранение формы дуг и отверстий при масштабировании по оси Y
- Детальная статистика по каждому корпусу
- Предупреждения о расхождениях с рекомендациями

### 🆕 Создание внешнего радиуса из внутреннего
- **Автоматическое создание** развертки внешнего радиуса на основе внутреннего
- Для случаев, когда конфигурация отверстий идентична
- Программа автоматически удлиняет развертку до нужной длины
- Сохранение всех отверстий, вырезов и дуг гибов
- Один клик: `create_outer_from_inner.bat`

## Структура проекта

```
FlatPatternStretcher/
├── app.py                              # Главное приложение с GUI
├── run.bat                             # Запуск приложения (Windows)
├── create_outer_from_inner.py          # 🆕 Создание внешнего из внутреннего
├── create_outer_from_inner.bat         # 🆕 Быстрый запуск
├── validate_step_vs_dxf.py             # 🆕 Валидация STEP ↔ DXF
├── requirements.txt                    # Зависимости
├── README.md                           # Документация
├── ИНСТРУКЦИЯ_АВТОМАТИЧЕСКАЯ_ОБРАБОТКА.md  # Детальная инструкция
├── ИНСТРУКЦИЯ_СОЗДАНИЕ_ВНЕШНЕГО_ИЗ_ВНУТРЕННЕГО.md  # 🆕 Инструкция
├── ИНСТРУКЦИЯ_ПРОВЕРКА_ШИРИНЫ.md           # 🆕 Проверка ширины
├── ШПАРГАЛКА_ВНЕШНИЙ_ИЗ_ВНУТРЕННЕГО.txt    # 🆕 Быстрая шпаргалка
├── ШПАРГАЛКА_ПРОВЕРКА_ШИРИНЫ.txt          # 🆕 Шпаргалка ширины
├── test_base_analyzer.py               # Тестовый скрипт
├── test_width_check.py                 # 🆕 Тест проверки ширины
├── analyze_test_files.py               # Анализ DXF файлов
└── core/
    ├── base_analyzer.py                # Анализ оснований
    ├── kompas_connector.py             # Подключение к КОМПАС-3D
    ├── dxf_processor.py                # Обработка DXF (ezdxf)
    └── flat_pattern_service.py         # Бизнес-логика
```

## Запуск

```bash
cd FlatPatternStretcher
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

> **Важно**: КОМПАС-3D V23+ должен быть установлен на той же машине.

## Как работает обработка

### Одиночный режим

1. Пользователь выбирает исходный файл, направление растяжения и вводит желаемую длину
2. Приложение автоматически:
   - Открывает файл в КОМПАС-3D
   - При необходимости экспортирует развертку в DXF
   - Измеряет текущую длину по выбранной оси (X или Y)
   - Автоматически определяет фиксированные зоны (дуги, окружности) и сохраняет их размеры
   - Распределяет изменение длины только по линейным участкам
   - Масштабирует растяжимые участки относительно выбранного центра
   - Сохраняет результат (DXF + отчёт)

### Пакетный режим (НОВОЕ!)

1. Пользователь выбирает папку с файлами
2. Нажимает **"1. Анализировать основания"**:
   - Программа находит все файлы оснований (`Основание*.dxf`)
   - Извлекает из каждого 2 основные дуги:
     - **Дуга 1** (больший радиус) → для внешнего радиуса
     - **Дуга 2** (меньший радиус) → для внутреннего радиуса
   - Вычисляет длины дуг
3. Нажимает **"2. Запустить обработку"**:
   - Программа находит все файлы радиусов
   - Автоматически сопоставляет каждый радиус с соответствующей дугой основания
   - Определяет тип (внешний/внутренний) и номер корпуса
   - Применяет нужную целевую длину (ось X)
   - Обрабатывает все файлы автоматически
   - **🆕 Автоматически проверяет ширину** после обработки
4. **🆕 Проверить ширину разверток** (опционально):
   - Детальная проверка ширины всех разверток
   - Показывает расхождения для каждого корпуса
   - Рекомендации по выравниванию
5. **🆕 Выровнять ширину** (если требуется):
   - Выбор эталонной ширины (внешний/внутренний радиус)
   - Автоматическое масштабирование по оси Y
   - Сохранение формы дуг и отверстий
6. Нажимает **"3. Создать отчёт"**:
   - Генерируется детальный отчёт со всей информацией о сопоставлениях

**Преимущества:**
- ✅ Нет ручного ввода целевых длин
- ✅ Исключены ошибки сопоставления
- ✅ Обработка любого количества корпусов
- ✅ Полная трассируемость в отчётах

---

## 🆕 Создание внешнего радиуса из внутреннего

Если у вас есть развертка **внутреннего радиуса**, но не получается создать **внешний**, и конфигурация отверстий идентична - программа может автоматически создать внешний радиус!

### Быстрый старт

1. Дважды кликните: **`create_outer_from_inner.bat`**
2. Укажите путь к файлу внутреннего радиуса
3. Введите номер корпуса
4. Готово! Программа создаст файл внешнего радиуса

### Пример

```
Файл: test/Внутренний радиус Г1.корп4 - 1шт.dxf
Корпус: 4
→ Результат: test/Внешний радиус Г1.корп4 - 1шт.dxf
```

**Программа автоматически:**
- Находит соответствующее основание
- Извлекает целевую длину (дуга 1)
- Удлиняет только прямые участки
- Сохраняет отверстия, вырезы и дуги гибов

**Подробнее:** `ИНСТРУКЦИЯ_СОЗДАНИЕ_ВНЕШНЕГО_ИЗ_ВНУТРЕННЕГО.md`

## Настройки

В файле `.env` (по желанию) можно задать:

```
KOMPAS_VISIBLE=1          # показывать/скрывать окно КОМПАС
DEFAULT_EXPORT_FOLDER=D:\DXF
```

## Лицензия

Внутренний инструмент компании ZVD. Использование вне компании запрещено без письменного разрешения.

